#include <stdio.h>
//write a program to find duplicate numbers in an array

int main(){
    int values[6], unique_numbers[6], duplicates[6];
    int i, j;



    for (i = 0; i <= 5; i++){
        printf("Enter a value for the array: ");
        scanf("%d", &values[i]);
    }
    i = 0;
    j = 0;

    for (i = 0; i <= 5; i++){}
        for (j = 0; j <= 5; j++
            if (values[i] == values[j]){
                printf("%d", values[i]);
            else
        }
    return 0;
        
    }